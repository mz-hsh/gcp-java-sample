apiVersion: v1
kind: Service
metadata:
  name: hsh-java-sample-service
  labels:
    app: hsh-java-sample
spec:
  type: LoadBalancer
  selector:
    app: hsh-java-sample
  ports:
    - port: 80
      targetPort: 8080
  
